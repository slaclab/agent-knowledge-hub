BACKEND_URL=http://localhost:8000
APP_URL=https://agent-knowledge-hub.slac.stanford.edu
SELF_PLUGIN_SLUG=agent-knowledge-hub
GITHUB_TOKEN=

# Shared secret for Next.js → backend trust — must match INTERNAL_API_SECRET in the backend
# Generate with: openssl rand -hex 32
# In k8s: injected via secretKeyRef from agent-knowledge-hub-secrets
INTERNAL_API_SECRET=
